type RANKS_ITEM = {
  self: string;
  stores: string;
  rank_bands: string;
  name: string;
  image: string;
  sort_order: number;
  gift_rate_min: number;
  gift_rate_max: number;
  band_amount_min: number;
  band_amount_max: number;
};

export type { RANKS_ITEM };
